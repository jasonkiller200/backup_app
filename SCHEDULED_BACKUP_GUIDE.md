# 台灣時區固定時間自動備份功能說明

本文檔說明如何使用優化版 Flask 多專案備份工具的固定時間自動備份功能。

## 功能特色

- ✅ **台灣時區支援** - 使用 Asia/Taipei 時區，避免時差問題
- ✅ **多時間點設定** - 可設定多個備份時間（如凌晨 2:00、中午 12:00）
- ✅ **避免重複備份** - 自動記錄每個時間點的備份日期，同一天不會重複執行
- ✅ **靈活的時間模式** - 支援「間隔模式」和「固定時間模式」切換
- ✅ **防誤差機制** - 允許 ±5 分鐘的時間誤差，避免剛好錯過時間點

## 使用方式

### 1. GUI 設定方式

1. **開啟程式**
   ```bash
   python flask_multi_backup.py
   ```

2. **切換到「備份設定」分頁**

3. **啟用自動備份**
   - 勾選「啟用自動備份」

4. **選擇備份模式**
   - **interval** (間隔模式): 每隔指定小時數自動備份
   - **fixed_time** (固定時間模式): 在指定時間點自動備份 ⭐推薦

5. **設定固定時間**（選擇 fixed_time 模式時）
   - 選擇小時（00-23）
   - 選擇分鐘（00, 15, 30, 45）
   - 點擊「➕ 新增」按鈕
   - 可新增多個時間點

6. **確認時區設定**
   - 預設為「Asia/Taipei」（台灣時間）
   - 可選其他時區：Asia/Tokyo、Asia/Seoul、Asia/Shanghai、UTC

7. **儲存設定**
   - 點擊「💾 儲存設定」按鈕

### 2. 建議的備份時間設定

針對不同的使用場景：

#### 🌙 深夜備份（推薦）
```
時間: 02:00
說明: 凌晨 2 點，此時伺服器負載最低，不影響白天工作
```

#### 🌅 清晨備份
```
時間: 06:00
說明: 早上 6 點，上班前完成備份
```

#### 🌃 多時段備份
```
時間: 02:00, 12:00, 18:00
說明: 一天三次備份，適合重要專案
```

#### 📅 工作日備份
```
時間: 01:30, 23:30
說明: 深夜備份，避開工作時間（09:00-18:00）
```

### 3. 手動編輯設定檔

也可以直接編輯設定檔：

```bash
# 設定檔位置
%APPDATA%\FlaskMultiBackup\config.json
```

設定範例：
```json
{
  "auto_backup": true,
  "backup_schedule_mode": "fixed_time",
  "backup_times": ["02:00", "14:00"],
  "timezone": "Asia/Taipei",
  "last_backup_dates": {
    "02:00": "2026-02-19",
    "14:00": "2026-02-19"
  }
}
```

### 4. 無聲模式自動備份

讓程式開機後在背景自動執行：

1. **設定固定時間備份**（如 02:00）
2. **使用 Windows 工作排程器**
   - 建立工作排程，在登入時啟動
   - 設定延遲 1 分鐘執行
   - 命令: `pythonw flask_multi_backup.py --silent`

3. **程式會**
   - 在背景執行（無視窗）
   - 每分鐘檢查是否到達備份時間
   - 到達時間後自動執行備份
   - 記錄備份日期避免重複執行

詳細設定請參考：[WINDOWS_AUTOSTART_GUIDE.md](WINDOWS_AUTOSTART_GUIDE.md)

## 運作原理

### 時間檢查邏輯

```
每分鐘檢查一次（fixed_time 模式）
    ↓
取得目前台灣時間
    ↓
檢查是否到達任一設定的備份時間（±5 分鐘誤差）
    ↓
檢查今天是否已經備份過
    ↓
如果條件滿足 → 執行備份
    ↓
記錄備份日期
```

### 誤差處理

- 允許 ±5 分鐘的時間誤差
- 例如設定 02:00，在 01:55-02:05 之間都會觸發
- 避免剛好錯過時間點而等到隔天

### 防重複機制

```python
# 設定檔中記錄每個時間點的最後備份日期
"last_backup_dates": {
    "02:00": "2026-02-19",
    "14:00": "2026-02-19"
}
```

- 每個時間點獨立記錄
- 同一天不會重複執行
- 跨年、跨月自動處理

## 時區說明

### 預設時區：Asia/Taipei (UTC+8)

- 台灣標準時間
- 無日光節約時間調整
- 全年 UTC+8

### 其他可用時區

| 時區 | 說明 | 與台灣時差 |
|------|------|-----------|
| Asia/Taipei | 台灣時間 | - |
| Asia/Tokyo | 日本時間 | +1 小時 |
| Asia/Seoul | 韓國時間 | +1 小時 |
| Asia/Shanghai | 中國時間 | 相同 |
| UTC | 世界協調時間 | -8 小時 |

### 時區自動偵測

如果系統不支援 zoneinfo（Python < 3.9），會自動降級使用系統本地時間。

## 常見問題

### Q: 為什麼到了設定時間沒有自動備份？

**檢查項目：**
1. 確認已勾選「啟用自動備份」
2. 確認「備份模式」選擇為「fixed_time」
3. 確認時間設定正確（24小時制）
4. 檢查備份目的地是否設定
5. 檢查是否有專案在列表中
6. 查看日誌檔案：`backup.log`

### Q: 可以設定幾個備份時間？

**A:** 沒有限制，可設定任意多個時間點。但建議不要超過 5-6 個，以免頻繁備份影響效能。

### Q: 備份時間會準確到分鐘嗎？

**A:** 程式每分鐘檢查一次，允許 ±5 分鐘誤差。例如設定 02:00，在 01:55-02:05 之間都會觸發。

### Q: 電腦在備份時間關機怎麼辦？

**A:** 如果電腦在備份時間關機：
- 程式在下次啟動時會檢查
- 如果今天還沒備份，會立即執行
- 如果今天已過，會等到明天設定時間

### Q: 如何知道自動備份是否成功？

**A:** 
1. 查看備份目的地是否有新的備份資料夾
2. 查看日誌檔案：`%APPDATA%\FlaskMultiBackup\logs\backup.log`
3. 查看錯誤日誌：`error.log`

### Q: 可以設定工作日的特定時間嗎？

**A:** 目前版本不支援「只備份工作日」功能。如需此功能，可：
- 手動在週末暫停自動備份
- 或使用 Windows 工作排程器設定更複雜的條件

### Q: 時區設定會影響現有備份嗎？

**A:** 不會。時區設定只影響自動備份的觸發時間，不影響已完成的備份檔案。

## 進階設定

### 自定義檢查頻率

編輯 `check_auto_backup` 方法中的檢查間隔：
```python
# 預設每分鐘檢查（fixed_time 模式）
check_interval = 60000  # 毫秒
```

### 修改時間誤差範圍

編輯 `_check_fixed_time_backup` 方法：
```python
# 預設 ±5 分鐘
if current_hour == hour and abs(current_minute - minute) <= 5:
```

### 添加新的時區選項

編輯 `setup_settings_tab` 方法：
```python
tz_combo = ttk.Combobox(
    tz_frame, 
    textvariable=self.timezone_var, 
    values=["Asia/Taipei", "Asia/Tokyo", "Asia/New_York", "Europe/London"],
    # ...
)
```

## 故障排除

### 問題：程式顯示「無法載入時區支援」

**解決方案：**
```bash
# 安裝 backports.zoneinfo（Python < 3.9）
pip install backports.zoneinfo
```

### 問題：時間顯示不正確

**檢查步驟：**
1. 確認系統時間正確
2. 確認時區設定為 Asia/Taipei
3. 檢查日誌中的時間戳記

### 問題：同一天重複備份

**可能原因：**
- 程式被重啟，導致記憶體中的日期記錄遺失
- 設定檔損壞

**解決方案：**
1. 檢查 `config.json` 中的 `last_backup_dates` 欄位
2. 如需重新備份，手動刪除該欄位

---

如有其他問題，請查看日誌檔案或手動執行測試。
